# Sample Environment Configuration for Android OTA Patcher Container
#
# Copy this file to .env and customize for your setup
# 

# Container runtime to use (podman or docker)
CONTAINER_RUNTIME=podman

# Container name and tag
CONTAINER_NAME=android-ota-patcher
CONTAINER_TAG=latest

# Data directory for downloads and working files
DATA_DIR=./data

# Keys directory containing signing certificates and keys
KEYS_DIR=./keys

# Default devices for CI pipeline (format: folder:codename)
DEVICES=cheetah:cheetah

# Enable debug output (1 for enabled, 0 for disabled)
DEBUG=0

# Magisk APK filename (should be in keys directory)
MAGISK_APK=Magisk-v29.0.apk

# AVB preinit device path (device-specific)
MAGISK_PREINIT_DEVICE=/dev/block/by-name/preinit

# Working directory inside container
WORKDIR=/data

# Chrome binary path (auto-detected in container)
CHROME_BINARY=/usr/bin/chromium-browser

# Optional: Custom chrome arguments for headless operation
CHROME_ARGS="--no-sandbox --disable-dev-shm-usage --disable-gpu"

# Optional: Selenium WebDriver options
SELENIUM_TIMEOUT=30
SELENIUM_IMPLICIT_WAIT=10

# Optional: Download settings
WGET_TIMEOUT=300
WGET_RETRIES=3
